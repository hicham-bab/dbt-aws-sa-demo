name: 'finance_aws'
version: '1.0.0'

require-dbt-version: [">=1.9.0"]

profile: 'finance_aws'

model-paths: ["models"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  finance:
    +schema: finance_aws
    +materialized: table
    +persist_docs:
      relation: true
      columns: true
